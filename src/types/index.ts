export type SlideContent = {
  image: string;
  title: string;
  content: string;
  slide: number;
};

export type LangParams = {
  lang: string;
};

export type Nav = {
  name: string;
  link: string;
};

export type CourseCard = {
  title: string;
  content: string;
  image: string;
  link: string;
};
